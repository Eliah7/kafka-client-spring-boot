spring:
  cloud:
    stream:
      kafka:
        binder:
          brokers: localhost:9092
      bindings:
        input:
          destination: quickstart-events
          contentType: application/json
        output:
          destination: quickstart-events
          contentType: application/json